name: Build and Test

on: 
  push:
    branches:  [main]
  pull_request:
    branches: [main]

jobs:
  build: 

    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v3
      - name: install
        run: |
          npm install
          npm run serve
          npm run build
